# otc-ecs-client

maintain ecs instances in OTC tenant

```shell
  -status string
    	ecs status (ACTIVE|SHUTOFF|FAILURE)
  -version
    	app version
  -vm string
        ecs name to get console logs
```
